name: publish-new-version
on: [push]
jobs:
  test-echo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: date > date.txt 
      - run: git status
      - run: git add .
      - run: git commit -m "github-action"
      - run: git push

